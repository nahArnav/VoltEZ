<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Local HTTP is allowed only in debug builds for testing against a LAN backend. -->
    <application android:usesCleartextTraffic="true" />
</manifest>
