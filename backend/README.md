# VoltEZ backend

FastAPI application and VoltEZ ML serving package used by the VoltEZ charging
marketplace. The Docker image is built from this directory, so the package
metadata intentionally references this local README.
